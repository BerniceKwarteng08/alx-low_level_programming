#include <stdio.h>
/**
 * main - reversal of alphabet
 * Return: 0 (good to go)
 */

int main(void)
{
	char k;

	k = 'z';
	while
		(k <= 'a')
		{
			putchar(k);
			k--;
		}
	putchar('\n');
	return (0);
}
