include "main.h"

/**
 * _isupper - check for upper cases
 * @c: number to be checked
 * Return: 1 (upper case) or 0 (anthing otherwise)
 */
int _isupper(int c)

{
	if (c >= 65 && c <= 90)
	{
	return (1);
	}
	return (0);
}
