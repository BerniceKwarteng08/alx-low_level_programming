#include "main.h"
#include <stdio.h>

/**
 * print_number - print an integer
 * @n: integer to be checked
 * Return: empty
 */
void print_number(int n)
{
	unsigned int g = n

		if (n < 0)
		{
			n *= -1;
			g = n;
			_putchar('_');
		}
	g /= 10;
	if (g != 0)
		print_number(k);
	_putchar(unsigned int)
}

